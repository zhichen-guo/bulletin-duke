<!DOCTYPE html>
<html class="blue-background">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Hello Bulma!</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
    </head>

    <body>
    <div class="onboarding-title">WELCOME TO <%= @interface.name.upcase %>!</div>

        <div class="container has-text-centered">
            <div class="column is-8 is-offset-2">
                <div class="box" id="volunteer-complete"">
                    <!-- <div class="onboard-arrow" id="volunteer-back-two"><i class="fas fa-arrow-left"></i></div> -->

                    <div class="column is-three-fifths is-offset-one-fifth">
                        <div class="container has-text-centered">
                            <div class="o-subtitle-done"> Congratulations! You've successfully signed up with <%= @interface.name %>. Click next to get started.
                            </div>
                            <figure class="onboarding-vector">
                                <%= image_tag "onboarding_complete.png"%>
                            </figure>
                        </div>
                    </div>

                    <% if current_user.type === "OrgAdmin" %>
                        <% homepage = "/organization/#{@organization.id}" %>
                    <% else %>
                        <% homepage = "/profile_volunteer" %>
                    <% end %>

                    <%= link_to homepage, class: "o-next-arrow" do %>
                        <i class="fas fa-arrow-right"></i>
                    <% end %>

                    <br>
                    <br>
                </div>
            </div>
        </div>
    </div>
    </body>
